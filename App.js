import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { StyleSheet, Text, View, Image, SafeAreaView } from 'react-native';

export default function App() {
  var flag = true;
  if (flag==true){    
    return (
   <SafeAreaView>

     {/**/} */}
   </SafeAreaView>
      
    );
}else{
  return(
    <Text>NULL</Text>

  );
}
}


const maq = StyleSheet.create({

});
